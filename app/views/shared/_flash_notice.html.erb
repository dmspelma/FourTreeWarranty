<% [:notice, :warning, :danger].each do |type| %>
  <% if flash.now[type] %>
    <div class = "alert alert-<%= type %> alert-dismissible fade show" role='alert'>
      <strong><%= flash.now[type]  %></strong>
    </div>
  <% end%>
<% end%>
